let clientSuccess = Boolean
let gitwitSuccess = Boolean

let clientAwarenessOfProblem = false

if (!clientAwarenessOfProblem) {
  let poorGoals
  if (poorGoals) {
    let poorSolutions
    if (poorSolutions) {
      clientSuccess = false
    }
  }
}

gitwitBusinessRelationshipOutcome = (clentSuccess) =>
  clientSuccess ? (gitwitSuccess = true) : (gitwitSuccess = false)

console.log(gitwitBusinessRelationshipOutcome(clientSuccess))
